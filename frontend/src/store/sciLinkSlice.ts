import { createSlice, PayloadAction } from "@reduxjs/toolkit";

export const initialState = {}

const sciLinkSlice = createSlice({
  name: "sciLink",
  initialState,
  reducers: {}
});


export default sciLinkSlice.reducer;
